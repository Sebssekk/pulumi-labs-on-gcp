apiVersion: v1
kind: Service
metadata:
  name: kafka
spec:
  ports:
    - port: 9092
      name: kafka-ext
    - port: 9093
      name: kraft
    - port: 19092
      name: kafka-int
  clusterIP: None
  selector:
    app: kafka
